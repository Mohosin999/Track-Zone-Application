import { useState } from "react";
import { mapValuesToState, mapStateToKeys } from "../utils/utils";

/**
 * Custom reusable form hook.
 *
 * @param {*} param0
 */
const useForm = ({ init, validate }) => {};

export default useForm;
